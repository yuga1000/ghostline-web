{
  "name": "ghostline-web",
  "version": "1.0.0",
  "description": "Static deployment of ghostline-web via Railway",
  "scripts": {
    "start": "node generate-env.js && serve ."
  },
  "dependencies": {
    "serve": "^14.2.4"
  }
}
